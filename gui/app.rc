/* app.rc -- application icon resource. The lowest-numbered ICON becomes the
 * executable's shell icon (shown in Explorer / taskbar). */
#define IDI_APPICON 101
IDI_APPICON ICON "../assets/logo.ico"
